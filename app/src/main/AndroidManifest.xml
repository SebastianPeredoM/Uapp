<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.softhy.udemy_seccion_04">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LifeCycleActivity" />
        <activity android:name=".ClickEventActivity" />
        <activity android:name=".KotlinAndroidExtensionsActivity" />
        <activity android:name=".PicassoActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".IntentsActivity" />
        <activity android:name=".PermissionsActivity" />
        <activity android:name=".SharedPreferencesActivity" />
        <activity android:name=".ExtensionsFunctionsActivity" />
        <activity android:name=".LifeCycleEventsActivity"></activity>
    </application>

</manifest>